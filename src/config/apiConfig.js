// export const API_CONFIG = {
//   // Change these URLs to match your backend deployment
//   BASE_URL: import.meta.env.VITE_API_URL || "http://localhost:8085",

//   // API endpoints
//   COURSES: "/api/courses",
//   COURSE_INSTANCES: "/api/instances",
// };
export const API_CONFIG = {
  BASE_URL: "http://localhost:8085",       // Your Spring Boot backend base URL
  COURSES: "/api/courses",                 // Endpoint for courses
  INSTANCES: "/api/instances"              // Endpoint for course instances
};